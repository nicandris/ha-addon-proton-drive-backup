# Python (vestigial — this project began as a Python integration)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Node — node_modules would be produced inside the Docker image at build time,
# so it is not committed. (There is no bundle step; Node runs src/ directly.)
proton_drive_backup/node_modules/

# Add-on runtime data / secrets — never commit.
**/session.json
data/

# IDE / editor
.idea/
*.iml
.junie/
out/

# OS
.DS_Store

# Local assistant guidance — not part of the published add-on
CLAUDE.md
